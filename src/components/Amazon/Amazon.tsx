import { FC } from 'react';
import styles from './index.module.scss';

type Props = {
  // itemImgUrl: string;
  // item: string;
  // review: string;
  // sale: string;
  // price: string;
  // descripution: string;
};
export const Amazon: FC<Props> = ({}) => (
  <div className={styles.container}>
    <div></div>
  </div>
);
